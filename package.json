{
  "name": "cm2040_midterm",
  "version": "1.0.0",
  "description": "A template for the mid-term assignment",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-db": "cat db_schema.sql | sqlite3 database.db #build a new database from the sql file",
    "build-db2": "type db_schema.sql | sqlite3 database.db",
    "clean-db": "rm database.db #remove the old database",
    "clean-db2": "rm database.db",
    "start": "node index.js",
    "dev": "nodemon index.js"
  },
  "author": "Shudhanshu Gunjal",
  "license": "ISC",
  "dependencies": {
    "dayjs": "^1.11.7",
    "ejs": "^3.1.8",
    "express": "^4.18.2",
    "express-validator": "^6.14.2",
    "sqlite3": "^5.1.2"
  },
  "engines": {
    "npm": ">=8.0.0",
    "node": ">=16.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
